# SPDX-License-Identifier: LGPL-3.0-or-later
# SPDX-FileCopyrightText: Copyright CERN for the benefit of the SHiP Collaboration
#
# Lines in the captured output that match one of these patterns are removed
# before comparison. Start a block with "test NAME", followed by its patterns.
# Use "test *" for patterns that apply to every test. In a pattern, "*"
# matches any text.
#
# Patterns must match the complete line. Keep them narrow: skipped lines are
# not validated.

test muonback-io
  *initialisation for run id *
  [INFO] Simulation RunID: *
  [INFO] Creating branch for * with address *
  Real time* s, CPU time* s
  [INFO] ---User path for Configuration (g4Config.C) is used: *
  [INFO] ---User path for Cuts and Processes (SetCuts.C) is used: *
  [INFO] Media file used: *
   * g4Config() using g4conf  macro: *
   @@@ G4ParticleHPInelasticData instantiated *
   @@@ G4ParticleHPInelastic instantiated *
   NeutronHP:*
   Creating field map called MainSpecMap using*
   [INFO] ShipBFieldMap::readMapFile() creating field MainSpecMap using file *
   Creating symmetric field map called muonShieldField using *
   [INFO] ShipBFieldMap::readMapFile() creating field muonShieldField using file *
   Info: Target using configuration: *

test reconstruction
  [INFO] Media file used: *
  Creating field map called MainSpecMap using *
  [INFO] ShipBFieldMap::readMapFile() creating field MainSpecMap using file *
  Creating symmetric field map called muonShieldField using *
  [INFO] ShipBFieldMap::readMapFile() creating field muonShieldField using file *
